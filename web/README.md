# Cost Estimating Web

React + TypeScript + Vite single-screen frontend.

```bash
npm install
npm run dev
```

The Vite development proxy forwards `/api` to `http://localhost:5080`.

The browser sends only `X-User-Email`. Role and project permissions are always resolved by the API.
